# Backend Environment Configuration

# API Configuration
DIALOG_API_HOST=0.0.0.0
DIALOG_API_PORT=8000
ADMIN_API_HOST=0.0.0.0
ADMIN_API_PORT=8001

# Ontology Paths (relative to backend directory)
ONTOLOGY_DIR=../ontologies

# CORS Configuration (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Audio Storage
AUDIO_STORAGE_DIR=../data/recordings
MAX_RECORDING_SIZE_MB=10

# Session Management
# Note: TTL values are loaded from ontologies, but can override here for testing
# SESSION_TTL_MINUTES=30
# ANSWER_TTL_MINUTES=10
# RECORDING_TTL_DAYS=7

# Database (Optional - for production use)
# DATABASE_URL=postgresql://user:password@localhost:5432/dialog_system
# REDIS_URL=redis://localhost:6379/0

# Security
# JWT_SECRET_KEY=your-secret-key-here
# JWT_ALGORITHM=HS256
# JWT_EXPIRATION_MINUTES=60

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Audio Processing
AUDIO_SAMPLE_RATE=16000
AUDIO_BIT_DEPTH=16
AUDIO_CHANNELS=1

# Speech Recognition (if using cloud services)
# GOOGLE_CLOUD_PROJECT_ID=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json

# Text-to-Speech (if using cloud services)
# TTS_SERVICE=google  # or 'azure', 'aws'
# TTS_API_KEY=your-api-key

# Monitoring (Optional)
# SENTRY_DSN=your-sentry-dsn
# PROMETHEUS_PORT=9090
